# Localización del correo - Compatible con Postfix
mail_location = mbox:/var/spool/mail/%u:INBOX=/var/spool/mail/%u

# Usuario y grupo para acceso al correo
mail_uid = 1000
mail_gid = 1000

# Configuración de namespace
namespace inbox {
  type = private
  separator = .
  inbox = yes
}